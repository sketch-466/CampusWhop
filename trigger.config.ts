import { defineConfig } from "@trigger.dev/sdk";

export default defineConfig({
  project: "proj_ekuafgrxxqgsywaosgun",
  runtime: "node-24",
  logLevel: "log",
  maxDuration: 300,
  dirs: ["./src/trigger"],
});